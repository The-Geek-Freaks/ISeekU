{
  "name": "iseeku-bridge",
  "version": "1.0.0",
  "description": "WebSocket-to-TCP bridge for ISeekU mobile clients",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/*.test.js"]
  },
  "engines": {
    "node": ">=18"
  }
}
